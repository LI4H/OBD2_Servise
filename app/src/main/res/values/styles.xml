<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="CustomOutlinedBox" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <!-- Цвет текста на рамке -->
        <item name="hintTextColor">@color/outline_hint_selector</item>
        <!-- Цвет обводки -->
        <item name="boxStrokeColor">@color/outline_selector</item>
        <item name="boxStrokeWidth">2dp</item>
        <!-- Отключаем анимацию подсказки -->
        <item name="hintAnimationEnabled">false</item>
        <!-- Всегда показываем подсказку -->
        <item name="hintEnabled">true</item>
    </style>


    <style name="CyberpunkNavButton" parent="Widget.AppCompat.Button">
        <item name="android:layout_width">120dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">monospace</item>
        <item name="android:letterSpacing">0.1</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/cyberpunk_button_bg</item>
        <item name="android:stateListAnimator">@null</item>
    </style>

    <style name="CyberpunkRadioButton">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">14sp</item>
        <item name="android:minHeight">48dp</item>
        <item name="android:buttonTint">?attr/colorSecondary</item>
        <item name="android:background">@drawable/cyberpunk_radio_background</item>
        <item name="android:paddingStart">8dp</item>
        <item name="android:paddingEnd">8dp</item>
    </style>

    <!-- Форма кнопок навигации -->
    <style name="NavButtonShape" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">8dp</item>
    </style>

    <!-- Стиль кнопок тем -->
    <style name="ThemeButton" parent="Widget.Material3.Button.OutlinedButton">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">12sp</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="strokeColor">?attr/colorPrimary</item>
        <item name="backgroundTint">@android:color/transparent</item>
        <item name="android:layout_margin">2dp</item>
    </style>

    <!-- Стиль кнопок языка -->
    <style name="LanguageButton" parent="Widget.Material3.Button">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">14sp</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="backgroundTint">#2A2A2A</item>
        <item name="android:layout_margin">4dp</item>
    </style>

</resources>