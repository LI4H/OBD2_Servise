<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/item_stat_block1"
    android:layout_width="match_parent"
    android:layout_height="100dp"
    app:cardBackgroundColor="@color/colorPrimaryVariant"
    android:includeFontPadding="false">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:paddingTop="4dp"
        android:paddingHorizontal="8dp"
        android:background="@drawable/st_fuel">

        <!-- Название блока -->
        <TextView
            android:id="@+id/stat_title1"
            android:text="@string/stat_title1"
            android:layout_width="wrap_content"
            android:layout_height="33dp"
            android:layout_gravity="center_horizontal|top"
            android:gravity="center"
            android:padding="0dp"
            android:layout_weight="0"
            android:textSize="14sp"
            android:textStyle="italic"
            android:textColor="@color/white"


            />

        <!-- Значение -->
        <TextView
            android:id="@+id/stat_value1"
            android:text="0.0"
            android:textSize="20sp"
            android:textColor="@color/white"
            android:textStyle="bold"
            android:gravity="center"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_weight="1" />

        <!-- Единица измерения -->
        <TextView
            android:id="@+id/stat_unit1"
            android:text="@string/stat_unit1"
            android:textSize="12sp"
            android:textColor="@color/white"
            android:gravity="center"
            android:layout_gravity="center_horizontal|bottom"
            android:layout_width="wrap_content"
            android:layout_height="33dp" />
    </LinearLayout>
</androidx.cardview.widget.CardView>
